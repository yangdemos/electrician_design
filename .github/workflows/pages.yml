name: GitHub Pages deployment disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - name: Deployment is disabled
        run: echo "GitHub Pages deployment is disabled."
